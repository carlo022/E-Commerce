{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "json-server-auth backend/data/db.json -r backend/data/routes.json --port $PORT --host 0.0.0.0",
    "dev": "vite",
    "build": "vite build",
    "server": "json-server-auth backend/data/db.json -r backend/data/routes.json --port 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "json": "^11.0.0",
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0",
    "server": "^1.0.42"
  }
}
